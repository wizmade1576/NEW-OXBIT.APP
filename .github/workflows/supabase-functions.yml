name: Deploy Supabase Functions

on:
  push:
    branches: ["main"]
    paths:
      - "supabase/functions/**"
      - ".github/workflows/supabase-functions.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Show Supabase CLI version
        run: supabase --version

      - name: Validate env
        run: |
          test -n "$SUPABASE_ACCESS_TOKEN" || (echo "SUPABASE_ACCESS_TOKEN is not set" && exit 1)
          test -n "$SUPABASE_PROJECT_REF" || (echo "SUPABASE_PROJECT_REF is not set" && exit 1)

      - name: Login to Supabase
        run: |
          supabase login --token "$SUPABASE_ACCESS_TOKEN"

      - name: Show functions directory
        run: |
          ls -la
          echo "--- supabase dir ---"
          ls -la supabase || true
          echo "--- functions dir ---"
          ls -la supabase/functions || true
          echo "--- function entries ---"
          ls -la supabase/functions/binance-proxy || true
          ls -la supabase/functions/news-proxy || true

      - name: Link project
        run: |
          supabase link --project-ref "$SUPABASE_PROJECT_REF"

      - name: Deploy functions
        run: |
          supabase functions deploy binance-proxy news-proxy --project-ref "$SUPABASE_PROJECT_REF"
